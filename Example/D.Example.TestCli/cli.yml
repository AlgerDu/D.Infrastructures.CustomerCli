tools:

cmds:
  show-word:
    win-service:
      condition:
        os: win
        type: service
      operations:
        install:
          inner-cmds:
            - "nssm install"
    docker:
      condition:
        type: docker
      operations:
        install:
          inner-cmds:
            - "nssm install"
      